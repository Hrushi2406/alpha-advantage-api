export type IDailyData = {
  date: string;
  open: string;
  high: string;
  low: string;
  close: string;
};

export type IMetadata = {
  information: string;
  symbol: string;
  lastRefreshed: string;
  outputSize: string;
  timeZone: string;
};
